/target
/dist
crates/frontend/dist
.env.*
# committed template the wasm build reads; real .env.frontend stays local
!.env.frontend.example

# terraform — .terraform.lock.hcl should be committed (like Cargo.lock)
terraform/.terraform/
terraform/terraform.tfstate
terraform/terraform.tfstate.backup
terraform/*.tfvars
terraform/*.tfvars.json

# ide / tools
.claude/
o.txt
samples/outputs
samples/inputs/images
playground