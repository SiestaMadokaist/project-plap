[package]
name = "dto"
version = "0.1.0"
edition = "2021"

[dependencies]
domain = { path = "../domain" }
pkg = { path = "../pkg"  }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
chrono = { version = "0.4", features = ["serde"] }
