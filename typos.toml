[files]
extend-exclude = [
    "target/",
    "dist/",
    "Cargo.lock",
    "terraform/.terraform/",
    # third-party API fixtures: full of random hex hashes, not real words
    "samples/inputs/",
]
[default.extend-words] 
tipe = "tipe"